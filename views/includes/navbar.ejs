<style>
    .search {
        background-color: #fe424d;
        color: white;
        border-radius: 2rem;
    }

    .search:hover {
        background-color: #fe424d;
        color: white;
    }

    .search i {
        display: inline;
        padding: 0.4rem;
    }

    .search_destinations {
        border-radius: 2rem;
        width: 35rem;
    }
</style>

<nav class="navbar navbar-expand-md bg-body-light border-bottom sticky-top">
    <div class="container-fluid">
        <a class="navbar-brand" href="#"><i class="fa-regular fa-compass"></i></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link active" href="/listings">Explore</a>
                </li>
            </ul>

            <div class="ms-auto">
                <form class="d-flex" role="search">
                    <input class="form-control me-2 search_destinations" type="search" placeholder="Search destinations"
                        aria-label="Search">
                    <button class="btn search"><i class="fa-solid fa-magnifying-glass"></i>Search</button>
                </form>
            </div>

            <div class="navbar-nav ms-auto">
                <li class="nav-item">
                    <a class="nav-link" href="/listings/new">Add New Listing</a>
                </li>
                <% if(!currUser) { %>
                    <a class="nav-link active" href="/signup">Sign Up</a>
                    <a class="nav-link" href="/login">Log in</a>
                    <% } %>
                        <% if(currUser) { %>
                            <a class="nav-link" href="/logout">Log out</a>
                            <% } %>
            </div>
        </div>
    </div>
</nav>